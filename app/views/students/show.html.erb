<h2><%= @student.name %></h2>
<h4>Courses:</h4>

<ul>
  <% @student.courses.each do |course| %>
  <section id="course-<%= course.id %>">
    <li><%= course.name %></li>
    <p>Grade: <%= @student.grade(course.id) %></p>
  </section>
  <% end %>
</ul>
